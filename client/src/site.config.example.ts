// Copy this file to site.config.ts (gitignored, like .env) and fill in this
// deployment's values. Pair with server/.env (DB_NAME/DB_HOST/...) and
// client/.env (VITE_API_URL) -- those two stay per-process/per-deployment
// too and can't be merged into this file. Values below are Plaridel's, as a
// worked example.
export const PROJECT_ID = 1;
export const SITE_NAME = 'Plaridel Extension';
export const SITE_TITLE = 'Royale Cold Storage — Plaridel Extension';
